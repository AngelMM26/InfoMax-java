# InfoMax 🔍

**InfoMax** is a lightweight search engine that crawls Wikipedia pages, indexes content using an inverted index, ranks results with PageRank, and serves them through a frontend built with JavaScript and styled CSS. While the Java version is still in progress, core components such as the crawler, indexer, and PageRank algorithm are fully functional. API endpoints for handling user queries are currently being finalized.
